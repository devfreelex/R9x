import { html } from '../../core/component'

const initConversation = (state) => {
	return html`
		<div class="umap-template umap-box">
			<h1>Init Conversation</h1>
		</div>
	`
}

export { initConversation }