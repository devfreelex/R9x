import { setStyle, css } from '../../core/component'

const mainStyle = (ctx) => { 

	return css`
		.app-main-wrapper {
			display:block;
			float:left;
			width:100%;
			height:100vh;	
		}
	`

	setStyle(style)
}

export { mainStyle }